{
  "name": "features-admin",
  "$schema": "../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "features-admin/src",
  "prefix": "lib",
  "projectType": "library",
  "tags": ["scope:features type:lib"],
  "targets": {
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "features-admin/jest.config.ts",
        "tsConfig": "features-admin/tsconfig.spec.json"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    }
  }
}
